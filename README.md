# ⏱️ TimeTracker

Простое, быстрое и современное веб-приложение для удобного отслеживания рабочего времени по задачам в течение дня.

## ✨ Ключевые возможности

- 🚀 **Быстрый старт**: Введите название задачи и нажмите «Создать» — карточка сразу создается, и включается таймер.
- ⏸️ **Управление таймером**: Кнопки старта/паузы для каждой задачи с правилом одного активного таймера (при запуске новой задачи предыдущая автоматически ставится на паузу).
- 🕒 **Детализация периодов**: Внутри каждой карточки отображается ненумерованный список периодов работы (`с ЧЧ:ММ по ЧЧ:ММ (длительность)`), с возможностью ручного добавления, редактирования и удаления интервалов.
- 📅 **Интерактивный календарь и история**: Переключение дней (Сегодня, Вчера, стрелки вперед/назад, календарь за месяц с подсветкой дней с записями).
- 📊 **Аналитика дня**: Наглядная диаграмма распределения времени по задачам и общий таймер за день.
- 🌓 **Темная и светлая тема**: Автоматическое переключение (System) или ручной выбор Dark / Light.
- 📥 **Экспорт данных**: Возможность выгрузки истории за день или диапазон в формате CSV.
- 🐳 **Развертывание в Docker**: Всё приложение (бэкенд FastAPI + фронтенд на React + база SQLite) собирается в единый легковесный Docker-контейнер.

---

## 🚀 Развертывание на сервере (Тип 2: Сборка из исходников)

В соответствии со структурой сервера (категория пользовательских приложений `applications`):

1. Скопируйте проект в каталог на сервере:
   ```bash
   mkdir -p ~/services/applications/time-tracker/data
   cd ~/services/applications/time-tracker
   ```

2. Создайте файл окружения `.env` (при необходимости скорректируйте порт):
   ```bash
   cp .env.example .env
   chmod 600 .env
   ```

3. Запустите сборку и контейнер:
   ```bash
   docker compose build
   docker compose up -d
   docker compose logs -f
   ```

4. **Резервное копирование (`backup.sh`)**:
   - Каталог `~/services/applications/time-tracker/` автоматически подхватывается ежедневным бэкапом `~/scripts/backup.sh`.
   - Так как используется SQLite, для надежности добавьте `time-tracker` в массив `STOP_START_SERVICES` в файле `~/scripts/backup.sh` (для корректной консистентной остановки/запуска контейнера во время снятия бэкапа).

---

## 🛠️ Локальный запуск для разработки

### 1. Бэкенд (Python / FastAPI)

```bash
# Создание виртуального окружения
python3 -m venv .venv
source .venv/bin/activate

# Установка зависимостей
pip install -r backend/requirements.txt

# Запуск тестов
pytest backend/tests/

# Запуск сервера разработки
cd backend
uvicorn app.main:app --reload --port 8000
```

### 2. Фронтенд (React / Vite)

В отдельном окне терминала:

```bash
cd frontend
npm install
npm run dev
```

Фронтенд будет доступен по адресу **[http://localhost:3000](http://localhost:3000)** (все запросы к `/api` автоматически проксируются на бэкенд `http://localhost:8000`).

---

## 📡 Спецификация REST API

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/api/tasks?date=YYYY-MM-DD` | Получить список задач и интервалов за выбранную дату |
| `POST` | `/api/tasks` | Создать задачу (`{"title": "...", "date": "YYYY-MM-DD", "auto_start": true}`) |
| `PATCH` | `/api/tasks/{task_id}` | Переименовать задачу (`{"title": "..."}`) |
| `DELETE` | `/api/tasks/{task_id}` | Удалить задачу и все её интервалы |
| `POST` | `/api/tasks/{task_id}/start` | Запустить/возобновить таймер задачи |
| `POST` | `/api/tasks/{task_id}/pause` | Поставить задачу на паузу |
| `POST` | `/api/tasks/{task_id}/intervals` | Вручную добавить интервал времени к задаче |
| `PUT` | `/api/intervals/{interval_id}` | Отредактировать время начала/окончания интервала |
| `DELETE` | `/api/intervals/{interval_id}` | Удалить интервал времени |
| `GET` | `/api/days` | Список дат со статистикой для календаря |
| `GET` | `/api/days/{date}/summary` | Сводка и список задач за день |
| `GET` | `/api/export/csv` | Экспорт истории в формате CSV |

---

## 📁 Структура проекта

```
timeTracker/
├── Dockerfile                  # Мультистейдж сборка (React SPA + FastAPI)
├── docker-compose.yml          # Запуск контейнера с монтированием тома данных
├── README.md                   # Документация проекта
├── backend/
│   ├── app/
│   │   ├── main.py             # Точка входа FastAPI, маршрутизация и статика
│   │   ├── config.py           # Конфигурация (порты, пути, БД)
│   │   ├── database.py         # Подключение к SQLite
│   │   ├── models.py           # SQLAlchemy модели (Task, TimeInterval)
│   │   ├── schemas.py          # Pydantic схемы валидации
│   │   ├── crud.py             # Бизнес-логика таймеров и запросов
│   │   └── routers/            # API эндпоинты (tasks, days, export)
│   ├── requirements.txt        # Python зависимости
│   └── tests/                  # Тесты API и бизнес-логики
└── frontend/
    ├── src/
    │   ├── App.tsx             # Главный интерфейс приложения
    │   ├── components/         # React-компоненты (TaskCard, DateNav, CalendarModal и др.)
    │   ├── api/client.ts       # REST API клиент
    │   └── utils/formatters.ts # Форматирование времени, дат и интервалов
    ├── package.json
    └── vite.config.ts
```
